# shellcheck shell=bash

gh.matrix() {
	local -a versions=()
	# m.git_tag_to_versions_array versions 'https://github.com/cli/cli' 'refs/tags/v'
}

gh.install() {
	local url="$1"
	local version="$2"

	echo "$url"
}
